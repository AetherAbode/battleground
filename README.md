# DeepSeek Proxy Server

A lightweight proxy that exposes DeepSeek's free web chat as an OpenAI-compatible API.

## Local Development

```bash
npm install
cp .env.example .env
# Edit .env with your DEEPSEEK_TOKEN
npm run dev
